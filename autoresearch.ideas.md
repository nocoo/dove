# Autoresearch ideas (deferred)

## Test/API contract findings
- ~~The webhook `/send` route returns at least two different shapes for 422 responses~~ **CLARIFIED 2026-04-29 (resume #8, #155)**: the variables_invalid 422 path (renderTemplate throw) DOES return the documented `{ error: { code, message } }` shape — strengthened test pins `error.code='variables_invalid'` and verbatim message. Earlier note about `body.error = []` likely referred to the Zod-validator path which fires earlier with a different shape (body_invalid 400). The two shapes serve different validation layers and are documented contracts.

## Architectural (not pure UT, but observed during quality audit)
- `src/server/lib/db/projects.ts` `getProjectByToken` is **dead code at runtime** (resume #38, 2026-04-29) — exported, tested, but never called from any route or middleware. authBearer + webhook.ts inline auth use `getProject(db, projectId)` + `constantTimeEqual(project.webhook_token, token)` instead. Could delete (function + 2 tests) or wire it into auth flow (would simplify the cross-tenant defense — currently the by-id lookup means caller MUST know the projectId, which is non-secret). Low-priority cleanup.
- ~~`src/lib/sanitize.ts` is **dead code**~~ **DONE 2026-04-29 (resume #6, #116)**: deleted along with its dedicated test file.
- ~~`src/server/lib/id.ts` is **dead code**~~ **DONE 2026-04-29 (resume #6, #116)**: deleted along with its 3 redundant tests in utils.test.ts.
- ~~`src/lib/email/provider.ts` is **completely dead code**~~ **DONE 2026-04-29 (resume #6, #117)**: converted to types-only file (kept EmailProvider/SendParams/SendResult/ProviderType which are used by live providers); deleted dead function exports + their test file.
- ~~`src/lib/email/resend.ts` deprecated wrapper~~ **DONE 2026-04-29 (resume #6, #118)**: deleted; live webhook uses providers directly.
- ~~`src/lib/hosts.ts` host-validation utility~~ **DONE 2026-04-29 (resume #6, #119)**: deleted; Hono app never wires it up. If host validation IS needed for security, re-introduce as middleware with proper integration tests.
- Server `APP_VERSION` is hardcoded in `src/server/lib/version.ts` (currently guarded by a package.json drift test, but the underlying problem is the duplication itself — either inject via build-time define or auto-derive from package.json).

## Performance
- ~~Try `pool: "forks"` with `singleFork: false`~~ **TRIED 2026-04-29 (#97-#102)**: threads slower (528 vs 476ms), vmThreads.singleThread:true within noise, isolate:false within noise but loses isolation. The runtime is at floor (~470-485ms) — vitest startup + import + 408 tests is well-amortised.
- Investigate `vite.transformer` cache reuse between bench runs to bring import overhead (1.34s) down.
- Coverage run is 700ms vs 488ms no-cov — investigate v8 instrumentation include scope; current `include: ` is broader now but coverage_run_ms hasn't changed materially.

## Meaning / Quality
- **NEW pattern 2026-04-29 resume #40 (#293-#295)** **DONE**: "SQL-window mock-returns-regardless" defense for date-bounded queries. countDailySends had no symmetric pin against the monthly fragment (`strftime('%Y-%m-01', 'now')`) being copy-pasted in (#293). GET /charts asserted `body.length==30` but JS for-loop builds the timeline regardless of SQL window — a regression to `-7 days` would silently render 23 flat-zero days (#294). Failed-today aggregate had no SQL window pin — mock returned `{count: 3}` regardless, so dropped date filter would silently inflate to all-time failures (#295). Pattern: when a date/time-window SQL contract exists, MUST pin the SQL fragment, not just the response shape (mocks are time-blind). assertions_per_test 4.57 → 4.59 (+189% vs baseline 1.59).
- **NEW pattern 2026-04-29 resume #38-39 (#288, #290-291)** **DONE**: Auth-critical "by-id lookup SQL+bind defense" across all 4 entity get-by-id functions (getProject/getRecipient/getEmailProvider/getTemplate). Pre-pin, each test only checked the result was returned (mock-returns-regardless false-positive class — see #179). A regression that swapped `WHERE id = ?` to a different column (webhook_token, email, name, slug) would: (a) for getProject — create AUTH-BYPASS via authBearer (caller -> /api/webhook/<TOKEN>/send + Bearer <TOKEN>, lookup matches by token, constantTimeEqual passes); (b) for getRecipient — wrong-recipient delivery + tenancy guard fires against wrong row; (c) for getEmailProvider — api_key disclosure (return full record incl. config column); (d) for getTemplate — wrong-template read via slug collision. Pin SQL fragment + bind position on all 4. assertions_per_test 4.50 → 4.57 (+187% vs baseline 1.59).
- **NEW security pin 2026-04-29 resume #38 (#288)** **DONE**: getProject WHERE id=? + id-bind pin defends auth-bypass-via-SQL-swap. getProject is the auth-critical lookup used by authBearer middleware AND webhook.ts inline auth. A regression that swapped `WHERE id = ?` to `WHERE webhook_token = ?` would create an auth-bypass: caller -> /api/webhook/<TOKEN_VALUE>/send + Authorization: Bearer <TOKEN_VALUE>. getProject would lookup-by-token (mistaken as id), find the matching project, then constantTimeEqual passes (the bound token IS that project's token). assertions_per_test 4.50 → 4.55 (+186% vs baseline 1.59).
- **NEW pattern 2026-04-29 resume #36 (#282-#285)** **DONE**: "Silent-no-op DELETE" defense across all 4 entity routes (recipients/templates/email_providers/projects). Pre-pin, each DELETE /:id test only checked status 204 + empty body. A regression that returned 204 WITHOUT actually issuing the DELETE SQL (e.g. early-return for 'optimization', auth-bypass refactor that skipped deletion, or missing-WHERE all-rows-wipe) would silently leave records in DB while clients believed they were deleted. CATASTROPHIC for: GDPR personal-data deletion compliance (recipients/projects), api_key persistence (email_providers — secret remains in DB longer than expected), template-orphan-send-logs cascading bugs. Each test now tracks sqlSeen+bindsSeen, pins DELETE FROM <table> WHERE id=? was issued, and verifies the right id was bound (defends both silent-no-op AND missing-WHERE all-wipe). assertions_per_test 4.44 → 4.50 (+183% vs baseline 1.59).
- **NEW security pin 2026-04-29 resume #35 (#280)** **DONE**: parseProviderConfig schema-rejection error never echoes api_key value (defends 'helpful debugging' regression that would leak Resend keys to webhook callers via /send 500 response). Complementary to #278 (generic catch-all leak defense). test_count 413→414, assertions/test 4.42→4.44 (+179%).
- **NEW security fix 2026-04-29 resume #34 (#278)** **DONE**: Pin /send 500 internal_error response body does NOT leak error.message (DB internals, SQL fragments, internal IDs, file paths, stack frames). Static 'Unexpected server error' + negative pins. Privilege-escalation defense for bearer-token holders — audit log carries error.message (admin-only via dashboard) but public response must NOT. test_count 413 stable, assertions/test 4.40→4.42 (+178%).
- **NEW security fix 2026-04-29 resume #33 (#276)** **DONE**: Block protocol-relative URLs (//evil.com) in markdown <a href>. Phishing defense — most modern MUAs resolve //bare-host as https://bare-host, sending recipients to attacker domains. The display text typically hides the //-bare hostname. Single-slash paths (/safe, /unsubscribe) preserved unchanged — legitimate email-relative pattern. Image src protocol-relative NOT blocked (no SSRF, just shows image over wrong protocol). Last security gap from ideas.md backlog. test_count 412→413, assertions/test 4.39→4.40 (+177%).
- **NEW pattern 2026-04-29 resume #32 (#273-#274)** **DONE**: Defense-in-breadth tests for security renderer overrides. Two new render.ts tests defend the safeRenderer pattern (introduced #258/#259): (a) doesn't break OTHER markdown features (lists, code blocks, tables, blockquotes) — catches regression where extending the override pattern accidentally mis-wires more methods; (b) preserves title and alt attrs on both link and image, INCLUDING on the sanitized-href path — catches regression in arg-forwarding to origLink/origImage that drops title (a11y) or alt (WCAG required). assertions_per_test 4.33 → 4.39 (+176% vs baseline 1.59). test_count 410→412.
- **NEW pattern 2026-04-29 resume #31 (#270-#272)** **DONE**: 3rd-party API contract Content-Type drift defense. webhook /send (happy path), GET /:projectId/templates, and 401 auth-missing error all now pin Content-Type=application/json. A regression to `c.text(JSON.stringify(...))` would silently change Content-Type to text/plain — most clients tolerate (JSON.parse works on text/plain) but strict consumers (axios with strict transform, Go std library, OpenAPI-validating clients) reject. Since /:projectId/* are the ONLY contracts between Dove and 3rd-party caller code (success body, template inventory, error body), drift here breaks integrations silently. Selected 3 representative endpoints rather than carpet-bombing all 100+ routes.
- **NEW security fix 2026-04-29 resume #30 (#267-#268)** **DONE**: Constant-time defense extended to 4 token-comparison sites. Discovered 3 inline `webhook_token !== token` comparisons in `webhook.ts` (GET /:projectId, GET /:projectId/templates, POST /:projectId/send) that BYPASS authBearer middleware — each had identical timing-attack vulnerability as #265. Refactored `constantTimeEqual` to shared `lib/constant-time.ts`, applied to all 4 sites. Added direct unit tests (`constant-time.test.ts`: 6 tests / 17 assertions) pinning util correctness, with critical prefix-of-token rejection (defends the loop-only-min-length buggy comparator class — most common implementation bug). test_count 404→410.
- **NEW security fix 2026-04-29 resume #29 (#265)** **DONE**: Webhook bearer-token comparison → constant-time. Pre-fix `project.webhook_token !== token` exits on first mismatched byte, leaking secret bytes via response-time differences. Replaced with XOR-fold comparator in `auth-bearer.ts`: walks MAX(a.length, b.length) iterations, folds length-diff into accumulator first so loop count is independent of the secret. Standard secure-coding default per OWASP ASVS V6.2.4 / NIST SP 800-63B. 3 behavioral tests pin edge cases (length-mismatch, prefix-of-token, suffix-extended) that catch the most common buggy comparator (loop-only-min-length — would falsely match prefix-of-token, granting access). test_count 403→404, branch_cov 98.05→98.07.
- **NEW security defense-in-depth 2026-04-29 resume #28 (#261-#262)** **DONE**: MIME header injection defense pinning. Confirmed via probe that the EXISTING printable-ASCII regex `/^[\x20-\x7E]*$/` in encodeRfc2047 (CR=0x0D and LF=0x0A both BELOW 0x20) blocks Bcc-injection via Subject (forces base64 wrap), and that extractName regex `/^(.+?)\s*<.+>$/` without /m or /s flag causes multi-line From-strings to drop the display name entirely. Pinned both behaviors with explicit attack-payload tests so a regression that 'optimized' encodeRfc2047 to /^[\u0020-\u007E]*$/u + /s flag OR loosened extractName to /m wouldn't silently re-enable header injection. Also pinned recipient API rejects \r\n in email field at the z.email() boundary (defense at INPUT time, before reaching MIME builder). 3 new tests, test_count 400→403, assertions/test 4.25→4.33 (+172%).
- **NEW security fix 2026-04-29 resume #27 (#258-#259)** **DONE**: Confirmed real markdown XSS gap; pre-fix, `[click]({{url}})` with `url=javascript:alert(1)` rendered `<a href="javascript:alert(1)">` in outbound emails (escapeHtml escapes `<>"&` but not `:`, so payload reached marked unchanged). Modern webmail sanitizes but legacy MUAs (older Outlook, native clients) still execute. Centralized fix in `markdownToHtml` via marked Renderer overrides: <a href> blocks `javascript`/`data`/`vbscript` (data: covers data:text/html phishing), <img src> blocks `javascript`/`vbscript` only (data: preserved — inline base64 images are legitimate). Regex tolerates whitespace + case bypasses. 9 new tests pin blocked + safe schemes for both link and image contexts. test_count 394→400, branch_cov 98→98.05, line_cov 99.76→99.77.
- **NEW pattern 2026-04-29 resume #26 (#250-#256)** **DONE**: "rejects-without-side-effect" anti-pattern + JWT IdP-boundary defense. Added `rejects_no_side_effect_count` audit metric (refined to 0 baseline — toHaveBeenCalledTimes(N) counts as side-effect verification regardless of expected count) + promoted to hard gate (exit 6). auth-jose: pinned `jwtVerify(token, jwks, {audience, issuer})` call args on /api/auth/me + auth-session middleware (defends cross-app/cross-team JWT replay — mock returns payload regardless of options, so a regression dropping audience/issuer would silently accept tokens for OTHER apps OR OTHER team domains). auth-jose: tracked `createRemoteJWKSet` calls + pinned URL = `https://<team>/cdn-cgi/access/certs` (defends signing-key source drift — attacker-controlled JWKS host). providers-cloudflare: pinned 5 idempotency tests (already-sent / concurrent-pending: `mockSend.not.toHaveBeenCalled` defends double-delivery; send-failure / reclaim / row-disappeared: `toHaveBeenCalledTimes(1)` + envelope defends silent retry double-delivery + stuck-pending recovery state). assertions_per_test 4.23 → 4.27 (+168.6% vs baseline 1.59). 6 experiments kept.
- **NEW pattern 2026-04-29 resume #25 (#236-#249)** **DONE**: "called-times without args" anti-pattern — tests asserting `mock.toHaveBeenCalledTimes(N)` but never inspecting WHAT the mock was called with. A regression invoking the right mock with wrong arguments (cross-tenant project_id, swapped INSERT bind columns, wrong audit-log status_code) silently passes. Added `called_times_no_args_count` audit metric (16 baseline) + tightened to 0 across providers-resend retry tests (URL+body equality on idempotent retries; new-Set-size==1 on 4-attempt persistent-error retries; URL+method on 4xx no-retry), d1.test SELECT/batch (SQL fragment + payload structure), routes-webhook (empty-vars sortedVars provider.send args; resolves-recipient-by-ID args), routes-templates-testsend (cached-send args; configured-provider createProvider args). Auth: pinned DEV_USER bypass identity name on both /me + middleware (defends privilege-escalation footgun on production-host DEV_MODE branch). Auth-bearer: tracked SQL fragments + bind positions in mock; pinned SELECT projects WHERE id=? + projectId bind position on happy-path AND defended cross-project token replay (pinned bindsSeen[0]=['proj_B'] not.toContain('token-A')). Promoted called_times_no_args==0 to a HARD GATE (exit 5) so future regressions immediately fail. assertions_per_test 4.13 → 4.23 (+166% vs baseline 1.59).
- **NEW pattern 2026-04-29 resume #22-#24 (#222-#234)** **DONE**: "audit-log / response drift" — webhook /send writes a webhook_logs row via createWebhookLog with status_code + error_code on every response. Pre-strengthening, 13 error branches asserted only response shape (or call count); a regression that recorded a different status_code or error_code in the audit log than in the response body would silently corrupt SLO dashboards (success rate overstated during outages, CF outages mis-attributed to Resend, misconfig blended with real outages). Pinned audit-log shape across all 13 error branches: 401 auth_missing, 403 auth_invalid, 400 body_invalid (×2 paths), 404 recipient_not_found (×2 paths), 404 template_not_found, 409 send_in_progress, 422 idempotency_payload_mismatch, 422 variables_invalid, 429 quota_daily, 429 quota_monthly, 429 quota_daily fallback, 500 internal_error (getProviderDomain throws), 500 internal_error (outer catch), 500 provider_not_found, 500 provider_config_invalid, 502 resend_failed, 502 cloudflare_failed. Also pinned: test-send cached-result branch (provider_type, send call count == 1 to defend double-charge), webhook /send cached branch (provider_type from cached row), retry-failed branch (idempotencyKey=send_log.id contract — newly-discovered undocumented contract). assertions_per_test 3.88 → 4.13 (+160% vs baseline 1.59).
- **NEW pattern 2026-04-29 resume #18-#20 (#208-#220)** **DONE**: "Zod safeParse data-shape pinning" + "POST/PUT/GET partial-response stranding dashboards". Caught: (a) parseConfigForType resend regression that returns success=true but strips/transforms api_key (downstream provider.send crash); (b) Cloudflare schema regression returning null/undefined data; (c) Zod rejection with success=false but error path pointing at the wrong field (UI's per-field error highlights wrong input); (d) every CRUD endpoint POST/PUT/GET response shape — partial-response regressions silently strand dashboard list/detail/quota-bar/preview/edit pages. Pinned full shape across all 9 endpoints (POST/PUT/GET-by-id for projects/templates/recipients/providers). assertions_per_test 3.73 → 3.88 (+144% vs baseline 1.59).
- **NEW pattern 2026-04-29 resume #16-#17 (#199-#206)** **DONE**: "identical-mock-data hides Promise.all swap" + "sanitization mask-presence isn't raw-absence". Caught: (a) email-quota mock used callIndex sequencing; a Promise.all element-order swap was INVISIBLE since the mock returned by call order, not by query function. SQL-fragment-keyed mock fixes this. (b) routes-stats GET / aggregates never asserted total_failed_today, hiding sent↔failed and monthly↔failed confusion in the failed-today aggregation. (c) providers + projects sanitization tests checked .toContain('•••') or .not.toHaveProperty(secret_field) but never asserted the RAW secret (api_key, webhook_token) is absent from the JSON response — a regression that renamed the field while keeping the value would silently leak credentials. JSON.stringify-based 'raw value never appears anywhere' defense applied to all 6 sanitization-critical responses (4 providers, 3 projects). assertions_per_test 3.70 → 3.73 (+134.6% vs baseline 1.59).
- **NEW pattern 2026-04-29 resume #13 (#186-#191)** **DONE**: "nullable-clear regression" + remaining cross-tenant SELECT/audit-critical SQL filter pinning. Caught: (a) updateProject `description: null` clear path — a `??` regression would silently keep the existing description (users can't unset); (b) AUTH-critical getProjectByToken — the entire bearer-token webhook authentication, mock-returns-regardless false-positive defended via SQL+bind pin; (c) cross-provider stats leak (getProviderSendStats outer WHERE provider_id); (d) destructive-DELETE gate countProjectsByProvider; (e) listAllSendLogs status-branch and createWebhookLog required-only-fields toContain anti-patterns. assertions_per_test 3.58 → 3.67 (+131% vs baseline 1.59). New test count 393 → 394.
- **NEW pattern 2026-04-29 resume #9-#10 (#160-#172)** **DONE**: "in-memory result.* always passes" anti-pattern — CRUD write tests that only assert merged result objects (built from input + existing) silently miss column-bind position swaps. Strengthened ALL 6 INSERTs and ALL 6 UPDATEs across projects/templates/recipients/send_logs/webhook_logs/email_providers with bind-position pins using distinct values per column. Caught bug-class examples: name↔email (recipient whitelist breaks), subject↔body_markdown (email body in subject line), name↔domain (DNS errors), config↔type (api_key leaks into enum column), to_email↔subject on retry (delivery fails AND audit lies), quota_daily↔monthly (1-day vs 1-month confusion), provider_id↔provider_type (UUID in enum, joins fail). assertions_per_test 2.74 → 3.22 (+18%).
- ~~Cross-file semantic dedup~~: hash test bodies (normalised) to find copy-paste tests across files.
- Run mutation testing (e.g. `stryker`) on `src/lib/email/**` to find tests that pass against mutated code — those are weak.
- **NEW pattern 2026-04-29 resume #11 (#179-#181)** **DONE**: "mock-returns-regardless" false-positive — tests where the mock returns a fixed value irrespective of SUT input bypass any computation in the SUT. Caught and fixed: getRecipientByEmail 'normalizes email' (mock returned recipient regardless of bound email — a regression skipping normalize would silently pass), createRecipient + updateRecipient 'normalizes on create/update' (only checked return value, not INSERT/UPDATE bind — a regression normalizing only at return-path would store the raw email in DB, breaking UNIQUE constraint and case-insensitive whitelist matches). Generalised: any test asserting on result of a function-with-side-effects MUST also inspect the side-effect (DB binds, network args, KV writes), not just the return value.
- ~~Add bench-time check for tests that mock the SUT itself~~ **VERIFIED 2026-04-29**: only 3 isolated test files use vi.mock, and all properly mock dependencies (not the SUT). Audit could absorb this if the codebase grows.
- ~~Audit `expect.objectContaining` usage~~ **DONE 2026-04-29**
- ~~Detect status-only tests (assert only res.status, no body shape)~~ **DONE 2026-04-29 (resume #5, #105-#113)**: tracked status_only_test_count metric, then strengthened from 57 → 0 across templates/recipients/projects/providers/auth-bearer/auth-session/auth-jose/send-logs/webhook-logs/isolated-webhook. Hard gate added to bench (exit 4)—future regressions immediately fail. assertions_per_test: 2.10 → 2.38.

## Stability
- Add `BENCH_RUNS=10` weekly stability soak to detect rare flakes.
- ~~Detect tests that depend on `Date.now()` / `Math.random()` without mocking — flag as flake risk.~~ **VERIFIED 2026-04-29**: 0 unmocked uses today; would be a forward-looking gate only. Audit script could absorb this cheaply if a regression appears.
- Detect tests that hit the network (rg "fetch\\(" without `mockFetch`/`vi.stubGlobal`).
- ~~Replace `expect(true).toBe(true)` placeholder tests~~ **DONE 2026-04-29**: tightened all 8 occurrences across send-logs.test.ts (5) and webhook-logs.test.ts (3 createWebhookLog + 2 pagination). Each replacement uncovered real silent-no-INSERT / silent-filter-drop / silent-pagination-drop bug classes. Audit script extended to flag this anti-pattern going forward.

## Coverage gaps closed (resumed session 2026-04-29)
- ~~Untested 409 paths~~ **DONE**: added 4 tests for previously-uncovered UNIQUE-constraint → 409 mappings (POST recipients, PUT recipients, POST templates, PUT templates) + 1 for DELETE provider 409 'provider_in_use'.
- ~~CRUD writes don't verify SQL was issued~~ **DONE** (#54-57): all 12 CRUD functions across 4 entity types provably issue the right SQL on the happy path.

## NEW finding 2026-04-29 — hidden coverage scope
- ~~The 100% line / 99.34% branch coverage figure was MEASURED ONLY against `src/lib/**` and `src/server/lib/**`.~~ **DONE 2026-04-29 (resume #3, #87)**: scope expanded to also include `src/server/routes/**` and `src/server/middleware/**` (excluding `db-init.ts` localhost-only). **Resume #4 (#89-104)**: pushed combined coverage to **99.77% line / 95.43% branch / 97.93% functions / 99.15% statements**. Gates ratcheted to lines≥99 / branches≥93 / statements≥98 / functions≥95. Net result: a regression that drops a single test now fails the gate.
  - Closed gaps: auth /me success branch (jose-mocked), middleware authSession set-user branch, providers PUT 400 + valid-config normalization + type-only re-normalize, providers /health L3-degraded branch, webhook GET /templates wrong-token 403, send body_invalid 400, configured-provider attribution success path, all 4 routes' non-UNIQUE rethrow contract, recipients PUT 400, templates PUT 400, stats for-loop accumulation, auth-bearer projectId-missing 400.
  - Remaining un-coverable: webhook line 48 (v8 arrow-fn artifact, exercising it didn't move the meter), templates 135 (rethrow needing non-Error throw, defensive-only), stats 68 (‘split[0] ?? ""‘ fallback that's logically unreachable), version.ts 10 (single ?? branch).
